{
  "buildCommand": "cd frontend && python -m pip install -q --break-system-packages -r requirements.txt && python -m ursus build",
  "outputDirectory": "frontend/output",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/:path*.html"
    },
    {
      "source": "/:path*/",
      "destination": "/:path*/index.html"
    }
  ]
}
