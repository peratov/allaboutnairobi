{
  "buildCommand": "cd frontend && python -m pip install --break-system-packages -r requirements.txt && /uv/python/versions/cpython-3.12.14-linux-x86_64-gnu/bin/ursus build",
  "outputDirectory": "frontend/output",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/:path*.html"
    },
    {
      "source": "/:path*/",
      "destination": "/:path*/index.html"
    }
  ]
}
