{
  "buildCommand": "cd frontend && python -m pip install --break-system-packages -r requirements.txt && export PATH=\"$(python -c 'import site; import os; print(os.path.dirname(site.getsitepackages()[0]))')/bin:$PATH\" && ursus build",
  "outputDirectory": "frontend/output",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/:path*.html"
    },
    {
      "source": "/:path*/",
      "destination": "/:path*/index.html"
    }
  ]
}
