{
	"name": "allaboutaccra",
	"version": "1.0.0",
	"private": true,
	"description": "Free guides and calculators for living in Accra. The site itself is generated by Ursus, a Python static site generator - this file exists so that Vercel's Node-centric zero-config detection finds a build to run.",
	"scripts": {
		"build": "bash scripts/vercel-build.sh",
		"dev": "python devserver.py 8814"
	}
}
